// index.js
import React from 'react'

const styles = {
  app: {
    paddingTop: 40,
    textAlign: 'center',
  },
}

export const Home = () => <div style={styles.app}>Weeelcome to React!</div>

console.log(`${ENV_API}`)

export default Home
